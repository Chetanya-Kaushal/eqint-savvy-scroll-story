{
  "name": "savvy-desktop",
  "version": "1.0.0",
  "description": "EQInt Savvy - Oracle HCM AI Assistant (Desktop Overlay)",
  "main": "src/main.js",
  "scripts": {
    "prestart": "node scripts/build-renderer.js",
    "start": "electron .",
    "build": "npm run prestart && electron-builder --win",
    "build:portable": "electron-builder --win portable",
    "test:e2e": "playwright test tests/",
    "test:unit": "vitest run tests/unit/"
  },
  "author": "EQInt",
  "license": "MIT",
  "dependencies": {
    "electron-store": "^8.1.0",
    "keytar": "^7.9.0",
    "openid-client": "^6.8.7"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "electron": "^30.0.0",
    "electron-builder": "^24.9.1",
    "esbuild": "^0.28.2",
    "playwright": "^1.62.1",
    "vitest": "^4.1.11"
  },
  "build": {
    "appId": "com.eqint.savvy-desktop",
    "productName": "EQInt Savvy",
    "win": {
      "target": "portable",
      "icon": "assets/icon.ico"
    },
    "portable": {
      "artifactName": "EQInt Savvy.exe"
    },
    "files": [
      "src/**/*",
      "knowledge/**/*",
      "assets/**/*"
    ]
  }
}
